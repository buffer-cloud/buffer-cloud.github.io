import fs from 'node:fs';
const data = JSON.parse(fs.readFileSync(new URL('./content.json',import.meta.url),'utf8'));
const escape = value => value.replaceAll('&','&amp;').replaceAll('"','&quot;').replaceAll('<','&lt;').replaceAll('>','&gt;');
const root = new URL('./dist/',import.meta.url);
let html=fs.readFileSync(new URL('index.html',root),'utf8');
const diagram = `<div class="architecture" aria-label="Proposed acquisition path: timer trigger to STM32H7 ADC to DMA and RAM buffer to planned ESP32 gateway to host"><span class="eyebrow">PROPOSED ACQUISITION PATH</span><div class="pipeline"><div><small>01 / TIMING</small><strong>Timer trigger</strong></div><span>→</span><div><small>02 / CAPTURE</small><strong>STM32H7 ADC</strong></div><span>→</span><div><small>03 / BUFFER</small><strong>DMA → RAM</strong></div></div><div class="planned-path"><span>PLANNED TRANSFER</span><strong>ESP32 gateway</strong><span>→</span><strong>Host interface</strong></div></div>`;
const projects=data.projects.map(p=>`<article class="project" id="${p.id}"><div class="project-top"><span class="project-number">${p.number} /</span><h3>${p.title}</h3><div class="project-intro"><p>${p.intro}</p><div class="tags">${p.tags.map(t=>`<span>${t}</span>`).join('')}</div></div></div><div class="project-visual${p.secondary?' paired':''}">${p.diagram?diagram:p.image?`<img src="${p.image}" alt="${escape(p.caption)}" loading="lazy" decoding="async">${p.secondary?`<img src="${p.secondary}" alt="PPS Monitor app screenshot from the internship report" loading="lazy" decoding="async">`:''}`:`<div class="pending-board"><span class="eyebrow">MCU DATA LOGGER / REV A</span><p>Layout verification</p><span>KiCad source → routing → fabrication checks</span></div>`}<span class="image-caption">${p.caption}</span></div><div class="project-bottom"><span class="project-status">${p.status}</span>${p.repo?`<a class="text-link" href="${p.repo}" target="_blank" rel="noopener noreferrer">Source code <span>↗</span></a>`:''}</div><details id="case-${p.id}"><summary>Explore the engineering</summary><div class="case-body">${p.sections.map(s=>`<div><h4>${s.title}</h4><p>${s.text}</p></div>`).join('')}${p.detailImage?`<figure><img src="${p.detailImage}" alt="${escape(p.detailCaption)}" loading="lazy"><figcaption>${p.detailCaption}</figcaption></figure>`:''}${p.download?`<div><h4>Fabrication outputs</h4><a class="text-link" href="${p.download}">Download Gerbers & drill files ↓</a></div>`:''}</div></details></article>`).join('\n');
html=html.replace(/<div id="projects">[\s\S]*?<\/div><!-- projects-end -->|<div id="projects"><\/div>/,`<div id="projects">${projects}</div><!-- projects-end -->`);
html=html.replace(/<div id="experience-list">[\s\S]*?<\/div><!-- experience-end -->|<div id="experience-list"><\/div>/,`<div id="experience-list"><div class="experience-row"><span class="date">2026 — PRESENT</span><div><h3>Iowa State University</h3><p>Undergraduate Research Intern</p></div><p>Embedded acquisition for pulsed-LED photoacoustic tomography.<br>Prof. Manojit Pramanik</p></div><div class="experience-row"><span class="date">JUN — JUL 2025</span><div><h3>IIT Hyderabad</h3><p>Summer Research Intern</p></div><p>ESP32 systems, power interfaces, and connected data visualization.<br>Prof. Rupesh Wandhere</p></div></div><!-- experience-end -->`);
html=html.replace(/<div id="stack-list">[\s\S]*?<\/div><!-- stack-end -->|<div id="stack-list"><\/div>/,`<div id="stack-list">${[['Hardware & measurement','KiCad · LTspice · PCB layout · Bench prototyping'],['Embedded development','C / C++ · STM32H7 · ESP32 · ADC / DMA / Timers · SPI / I²C'],['Connected systems','W5500 Ethernet · WebSockets · Wi-Fi · BLE · Bluetooth Classic'],['Interfaces & visualization','Flutter / Dart · TFT displays · Touch interfaces · CSV export']].map(([h,p])=>`<div class="stack-row"><h3>${h}</h3><p>${p}</p></div>`).join('')}</div><!-- stack-end -->`);
html=html.replace(/<div id="about-copy">[\s\S]*?<\/div>/,`<div id="about-copy"><p>I’m an Electrical Engineering student in the BITS Pilani–Iowa State University dual-degree program, based in Ames, Iowa.</p><p>My work moves between circuit design, embedded programming, and the interfaces that make measurements useful. I’m interested in how the whole path fits together—from a signal on a board to data someone can understand.</p><p class="eyebrow">B.E. ELECTRONICS & COMMUNICATION + B.S. ELECTRICAL ENGINEERING<br>2024–2028 PROGRAM</p></div>`);
html=html.replace(/<div id="contact-links">[\s\S]*?<\/div>/,`<div id="contact-links"><a href="mailto:amlesh08@iastate.edu">amlesh08@iastate.edu ↗</a><a href="https://github.com/buffer-cloud" target="_blank" rel="noopener noreferrer">GitHub ↗</a><a href="https://www.linkedin.com/in/amlesh-swarup-sahoo-aab98a362" target="_blank" rel="noopener noreferrer">LinkedIn ↗</a><a href="assets/Amlesh-Sahoo-Resume.pdf" target="_blank" rel="noopener noreferrer">Resume ↗</a></div>`);
fs.writeFileSync(new URL('index.html',root),html);
console.log(`Built ${data.projects.length} project case studies into static HTML.`);
